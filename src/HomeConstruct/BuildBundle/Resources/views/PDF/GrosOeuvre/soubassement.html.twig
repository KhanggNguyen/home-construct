<h3>
    Informations sur le soubassement
</h3>
<div class="block_profile_with_estimation">

    <div class="col-lg-9 g-mb-50">
        <!-- Info -->
        <div class="g-brd-around g-brd-gray-light-v4 rounded g-pa-30 g-mb-30">

            <div class="row">
                <div class="col-12">
                            <span class="d-block">Type :
                                <b>
                                    {% if soubassement.type is defined and soubassement.type is not null %}
                                        {{ soubassement.type.nom }}
                                    {% else %}
                                        Non renseigné
                                    {% endif %}
                                </b>
                            </span>
                </div>
            </div>
            <div class="row">
                <div class="col-12">
                    <span class="d-block">Prix du forfait par rapport au type :
                        <b>
                            {% if soubassement.type is defined and soubassement.type is not null %}
                                {{ soubassement.type.prixForfait|number_format(2, ',', ' ') }} €/m3
                            {% else %}
                                Non renseigné
                            {% endif %}
                        </b>
                    </span>
                </div>
            </div>
        </div>
        <!-- End Info -->

    </div>
    <div class="block_estimation_prix">
        <h4 class="titre_estimation text-center">
            Estimation
        </h4>
        <h6> <I> Données utilisés : </I></h6>
        <ul class="liste_variables_estimation">
            <h5 class="titre_sous_entite">
                <div class="link_sous_entite">
                    Informations de base :
                </div>
            </h5>
            <li>
                {% if soubassement.grosOeuvre.informationBase.surfaceTotale is defined and soubassement.grosOeuvre.informationBase.surfaceTotale is not null %}
                    <i class="fas fa-check-circle variable-presente"></i>
                    Surface totale de la maison : <b>{{ soubassement.grosOeuvre.informationBase.surfaceTotale|number_format(2, ',', ' ') }}m²</b>
                {% else %}
                    <i class="fas fa-times variable-non-presente"></i>
                    Surface totale de la maison
                {% endif %}

            </li>
            <h5 class="titre_sous_entite">
                <div class="link_sous_entite">
                    Informations Etude du sol :
                </div>
            </h5>
            <li>
                {% if soubassement.grosOeuvre.etudeSol is defined and soubassement.grosOeuvre.etudeSol is not null %}
                    {% if soubassement.grosOeuvre.etudeSol.profondeurChoisie is defined and soubassement.grosOeuvre.etudeSol.profondeurChoisie is not null %}
                        <i class="fas fa-check-circle variable-presente"></i>
                        Profondeur (Etude du sol) : <b>{{ soubassement.grosOeuvre.etudeSol.profondeurChoisie }}m</b>
                    {% endif %}
                {% else %}
                    <i class="fas fa-times variable-non-presente"></i>
                    Profondeur : <I> Non renseigné</I>
                {% endif %}

            </li>
            <h5 class="titre_sous_entite">
                <div class="link_sous_entite">
                    Informations Soubassement :
                </div>
            </h5>
            <li>
                <i class="fas fa-check-circle variable-presente"></i>
                Prix du type de soubassement : <b>{{ soubassement.type.prixForfait }}€/m3</b>
            </li>
        </ul>
        {% if soubassement.prixTotal is defined and soubassement.prixTotal is not null %}
            <div class="block_affichage_prix text-center font-weight-bold g-font-size-30 border-dark border">
                {{ soubassement.prixTotal|number_format(2, ',', ' ') }} €
            </div>
        {% else %}
            <I>
                Pour pouvoir estimer le prix total veuillez renseigner les données manquantes (marquées d'une croix rouge)
            </I>
        {% endif %}
    </div>
</div>