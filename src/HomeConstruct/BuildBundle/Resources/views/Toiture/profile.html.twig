{% extends "@HomeConstructBuild/GrosOeuvre/base-forms-profiles-grosOeuvre.html.twig" %}

{% block content %}
    {% block button_return %}
        {{ parent() }}
    {% endblock button_return %}

    {% block sub_content %}
        <div class="block_profile_with_estimation">
                <div class="col-lg-9 g-mb-50">
                    <!-- Info -->
                    <div class="g-brd-around g-brd-gray-light-v4 rounded g-pa-30 g-mb-30">

                        <div class="row">
                            <div class="col-12">
                                <span class="d-block">Type de couverture :
                                    <b>
                                        {% if toiture.typeCouverture is defined and toiture.typeCouverture is not null %}
                                            {{ toiture.typeCouverture.nom }}
                                        {% else %}
                                            Non renseigné
                                        {% endif %}
                                    </b>
                                </span>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-12">
                                <span class="d-block">Prix du type de couverture :
                                    <b>
                                        {% if toiture.typeCouverture is defined and toiture.typeCouverture is not null %}
                                            {{ toiture.typeCouverture.prix|number_format(2, ',', ' ') }}€
                                        {% else %}
                                            Non renseigné
                                        {% endif %}
                                    </b>
                                </span>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-12">
                                <span class="d-block">Nombre de mètre carré :
                                <b>
                                        {% if toiture.m2 is defined and toiture.m2 is not null %}
                                            {{ toiture.m2|number_format(2, ',', ' ') }}m²
                                        {% else %}
                                            Non renseigné
                                        {% endif %}
                                    </b>
                                </span>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-12">
                                <span class="d-block">Degré de la pente :
                                <b>
                                        {% if toiture.degrePente is defined and toiture.degrePente is not null %}
                                            {{ toiture.degrePente }}°
                                        {% else %}
                                            Non renseigné
                                        {% endif %}
                                    </b>
                                </span>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-12">
                                <span class="d-block">Exposition aux vents :
                                <b>
                                        {% if toiture.expoVent==true %}
                                            Oui
                                        {% else %}
                                            Non
                                        {% endif %}
                                    </b>
                                </span>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-12">
                                <span class="d-block">
                                    Tarif de la main d'oeuvre :
                                <b>
                                    {% if toiture.tarifMainDoeuvre is defined and toiture.tarifMainDoeuvre is not null %}
                                        {{ toiture.tarifMainDoeuvre|number_format(2, ',', ' ') }} €/m²
                                    {% else %}
                                        Non renseigné
                                    {% endif %}
                                </b>
                                </span>
                            </div>
                        </div>
                        <div class="text-center">
                            <a id="button_next"
                                    class="btn btn-xl u-btn-lightblue-v3 g-width-160--md g-font-size-default g-ml-10"
                                    href="{{ path('home_construct_toiture_edit',{'idToiture':toiture.id}) }}" data-speed="350">
                                Modifier
                                <div  class="fas fa-pencil"></div>
                            </a>
                        </div>
                    </div>
                    <!-- End Info -->
                </div>
            <div class="block_estimation_prix">
                <h4 class="titre_estimation text-center">
                    Estimation
                </h4>
                <h6> <I> Données utilisés : </I></h6>
                <ul class="liste_variables_estimation">
                    <h5 class="titre_sous_entite">
                        <a class="link_sous_entite" href="{{ path('home_construct_information_base_profile',{'id':toiture.grosOeuvre.informationBase.id}) }}">
                            Informations sur la toiture :
                        </a>
                    </h5>
                    <li>
                        {% if toiture.m2 is defined and toiture.m2 is not null %}
                            <i class="fas fa-check-circle variable-presente"></i>
                            Surface totale de la toiture : <b>{{ toiture.m2 |number_format(2, ',', ' ') }}m²</b>
                        {% else %}
                            <i class="fas fa-times variable-non-presente"></i>
                            Surface totale de la toiture : <b><I> Non renseigné </I></b>
                        {% endif %}

                    </li>
                    <li>
                        {% if toiture.tarifMainDoeuvre is defined and toiture.tarifMainDoeuvre is not null %}
                                <i class="fas fa-check-circle variable-presente"></i>
                                Tarif de la main d'oeuvre : <b>{{ toiture.tarifMainDoeuvre|number_format(2, ',', ' ') }} €/m²</b>
                        {% else %}
                            <i class="fas fa-times variable-non-presente"></i>
                            Tarif de la main d'oeuvre : <b><I>Non renseigné</I></b>
                        {% endif %}
                    </li>
                    <li>
                        {% if toiture.typeCouverture.prix is defined and toiture.typeCouverture.prix is not null %}
                            <i class="fas fa-check-circle variable-presente"></i>
                            Prix du type de couverture : <b>{{ toiture.typeCouverture.prix|number_format(2, ',', ' ') }} €</b>
                        {% else %}
                            <i class="fas fa-times variable-non-presente"></i>
                            Prix du type de couverture : <b><I>Non renseigné</I></b>
                        {% endif %}
                    </li>
                </ul>
                {% if toiture.prix is defined and toiture.prix is not null %}
                    <div class="block_affichage_prix text-center font-weight-bold g-font-size-30 border-dark border">
                            {{ toiture.prix|number_format(2, ',', ' ') }} €
                    </div>
                    {% else %}
                    <I>
                        Pour pouvoir estimer un prix total veuillez ajouter les entités manquantes (marquées d'une croix rouge)
                    </I>
                {% endif %}
            </div>
        </div>
    {% endblock sub_content %}

{% endblock content %}

